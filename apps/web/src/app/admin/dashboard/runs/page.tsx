'use client';

import AdminPagePlaceholder from '@/components/admin-page-placeholder';

export default function AdminRunsPage() {
    return (
        <AdminPagePlaceholder
            title="Runs Management"
            description="Run statistics and history management features are currently being built."
        />
    );
}
