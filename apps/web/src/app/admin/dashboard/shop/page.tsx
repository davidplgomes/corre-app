'use client';

import AdminPagePlaceholder from '@/components/admin-page-placeholder';

export default function AdminShopPage() {
    return (
        <AdminPagePlaceholder
            title="Shop Management"
            description="Manage Corre shop items, inventory, and redemption settings."
        />
    );
}
